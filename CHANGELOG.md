# Changelog

## Unreleased

- 검정 테마에서 투명 PNG 로고가 등록 목록에 묻히지 않도록 썸네일에 고정 체크 배경 추가 — 저장된 로고 색·투명도와 합성 결과는 유지
- 인스탁스 스퀘어·와이드를 제거하고 기존 단일 인스탁스를 포함한 8종 프레임 선택으로 정리 — 이전 작업·프리셋은 저장된 배치 값을 인스탁스로 이어받음
- 인스탁스를 미니·스퀘어·와이드 선택으로 나눠 10종 프레임을 제공 — 사진 비율을 유지하는 규격별 여백을 공통 렌더로 그리며 기존 인스탁스 결과·설정·프로젝트·프리셋은 미니로 보존
- 미리보기 상단 구분선·소개·내장 예제와 특정 제목·서명 예시를 제거하고 파일명 규칙을 처음부터 안내 — 내보내기는 전체·현재 장 중심으로 줄이되 이전 자동 비율·패딩색·크기 저장값 보존, 인스탁스는 흰색으로 통일하고 EXIF 채택 후 해당 사진 범위 표시
- 여백 색 설정을 기본으로 접힌 패널로 바꿔 설정 영역을 줄이고 열림·닫힘 상태를 브라우저에 기억
- 프레임 크기 드래그를 시작 시 화면 배율 기준으로 계산해 매트·키라인 등에서 여백이 출렁이는 문제를 수정하고, 사진 둘레 여백 슬라이더를 기존 숫자 입력·프로파일·배치 되돌리기와 연결
- 사진 스트립의 중복 상태 줄과 삭제 취소를 제거하고 순서 이동을 썸네일 아래 작은 화살표 버튼으로 이동 — 터치·키보드 조작을 유지하며 삭제한 사진의 디코드 객체를 즉시 해제
- 부분 프로젝트 복원 중 편집·저장 진행 중 새 편집의 dirty 상태를 보존하고, 연결되지 않은 원본 항목까지 저장·재복원하며 복구·저장 회귀 검사 추가
- 기존 공통 렌더에 균등 매트·갤러리·키라인·미니멀 EXIF를 추가해 8종을 제공하고 현재 사진으로 스타일 썸네일 표시
- 여백 프레임에 흰색 기본·흰색/검정/아이보리·색상 선택기·HEX와 자동 대비를 추가하고 비율 패딩의 프레임 색 따르기·직접 지정을 저장 — 기존 밝음/어두움과 필름·인스탁스 결과 보존
- 카메라·날짜만 있는 부분 EXIF와 스캐너 의심 정보를 명시적으로 채택하고 수동값을 유지하며 조리개·셔터·ISO·초점거리 표시 및 날짜 형식 선택 지원
- 선택 N장 내보내기, 대상별 크기·비율·넘침 사전 확인, 사진 사이 취소와 실패 재시도를 추가하고 원래 스트립 번호·출력 설정 유지
- 촬영 정보를 제외하는 로컬 디자인 프리셋, 좁은 화면 고정 미리보기·핀치 줌·요소 정밀 이동을 추가하고 기존 배치 되돌리기와 연동
- Node 동작 회귀 검사를 배포 전 검사에 연결하고 Chrome 신선 초기화·기존 프레임 픽셀 일치·모바일 폭 검증 및 24MP 합성 JPEG 1/6/12장 측정 조건·실기기 한계를 기록
- 설정 패널의 새 기본 너비를 500px로 넓히고 특정 카메라·렌즈·필름 입력 예시와 예전 배포본이 자동 저장한 장비 시드를 제거 — 사용자가 직접 저장한 다른 프리셋과 기존 너비 설정은 유지
- 작업 중에도 보이는 사진 추가 버튼, 프로젝트 저장 필요·마지막 저장 상태와 이탈 경고, 사진 삭제 취소를 추가하고 기존 레이아웃 실행 취소를 `배치 되돌리기`로 명확히 구분
- 프로젝트 원본은 파일명·크기·수정 시각이 모두 맞을 때만 자동 연결하며, 파일명만 같은 원본은 차이와 연결 여부를 확인하고 다운로드 폴백은 `저장 완료`가 아닌 `다운로드 요청`으로 표시
- 현재 사진·선택 장수·촬영 정보 적용 범위와 스타일·방향별 공통 배치를 구분하고, 현재 프레임에 필요한 설정만 노출하며 프레임 미니 예시와 HTML 내장 로컬 체험 사진을 추가
- 좁은 화면의 미리보기↔설정 이동 버튼과 터치·키보드 사진 순서 버튼을 추가하고 입력 행 높이를 줄이되 주요 조작 영역 48px과 키보드 접근성을 유지
- Instagram 공식 3:4 사진 지원 공지를 반영해 3:4 고정 비율과 3:4~1.91:1 자동 모드를 제공하고 게시 경로별 차이를 안내
- 스트립 썸네일을 180px 로컬 축소본으로 분리하고 원본 디코드 객체의 삭제 수명을 관리하며, 1·12·36장 로딩 측정 조건과 한계를 문서화
- 의존성 없는 `tools/check.mjs` 회귀 검사를 추가하고 GitHub Pages 배포 전에 구문·DOM·공통 렌더 경로·외부 요청 부재·구버전 프로젝트·legacy 무변경을 검사
- 개인 설정에 화이트/블랙 화면 테마를 추가하고 선택을 브라우저에 저장 — 무채색 UI만 전환하며 강조색·사진·합성 결과는 유지
- UI를 따뜻한 미색 캔버스·흰 작업면·코발트 강조·노출된 구조선 중심의 Structural Modernism으로 개편하고, 1100px/720px 반응형과 모바일 2행 줌 도구막대를 적용
- 썸네일 선택·삭제 영역과 팔레트 삭제 버튼을 분리하고 키보드 초점·개인 설정 닫기를 보강했으며, 실제 줌바 높이를 반영해 화면 맞춤 사진이 가려지지 않도록 수정
- 촬영 정보에 전역 기본값/현재 장/선택 N장 범위를 추가해 카메라·렌즈·필름·날짜·설정·메모와 표시 여부를 장별 또는 여러 장에 완전히 오버라이드하고 EXIF·전역값으로 항목별 복귀 지원
- 폴더 내보내기 시 기존 파일을 덮어쓰지 않고 ` (2)`, ` (3)` 순번으로 보존하며 진행률·현재 파일·실패·자동 축소·이름 변경 결과를 표시
- 장비·레이아웃 프리셋을 검증된 전용 JSON으로 내보내고 기존 목록과 안전하게 병합해 다른 기기로 이동하는 기능 추가
- 렌즈 없이 노출값만 있는 부분 EXIF 인식, 키보드 드롭존·폼 접근성 이름·실시간 상태 안내를 보강하고 README 결과 예시와 영문 요약 추가
- 이미지 로고 크기가 프레임별 여백 한도에 조용히 제한되지 않고 설정한 30~300% 배율을 일관되게 적용하도록 수정하고, 넘침은 기존 빨간 경계 경고로 안내
- 미사용 코드와 상쇄되는 CSS를 제거하고 사진 방향 판별·프로파일 키·표시 설정 수집을 단일화해 단일 HTML 내부 구조를 정리 (렌더 결과·저장 형식 유지)
- 요소 이동·프레임 크기·레이아웃 수치·프리셋·초기화 직전 상태를 프레임 스타일별 최대 50단계 복원하는 `돌아가기` 버튼 추가, 인스탁스 우상단 그립은 왼쪽·아래로 끌면 여백이 커지도록 방향 반전
- 기본 시스템 글꼴과 분리된 PC 설치 글꼴 선택(데스크톱 Chromium) 및 TTF·OTF·WOFF·WOFF2 파일 글꼴 추가 — 영문·한글을 문자권별로 렌더하고, 사용 중인 파일 글꼴은 용량 검사 후 프로젝트에 포함해 복원
- 프레임 스타일마다 세로·가로 레이아웃을 독립 저장해 스타일 전환 시 여백·요소 위치가 섞이지 않도록 수정하고, 가로 인스탁스 회전 글씨의 개별 이동 지원
- 하단 사진 스트립의 가로·세로 썸네일을 원본 비율에 맞춰 사진 전체가 보이도록 개선
- 첫 화면 안내문의 폭과 줄바꿈 균형을 조정해 짧은 어절이 홀로 다음 줄에 남지 않게 개선
- 사진 순서·설정·장별 EXIF 수정값을 사진 원본 없이 `.photoframe.json`으로 저장하고, 원본 재선택으로 복원하는 프로젝트 파일 추가
- 테두리 여백(인스탁스) 조정을 사진 우상단 모서리 그립으로 변경
- 조정 부위 호버 강조 + 방향 커서 (박스 이동/모서리/선)
- 경계 표시 기본 켜짐 — 줌바 '경계'로 끄면 선택이 기억된다

## v1.0.0 — 2026-07-14

첫 공개 릴리즈. 프로토타입(v3 단일 HTML)에서 출발해 실사용 수준으로 확장.

### 프레임
- 필름 스트립 / 인스탁스 / 하단·상단 여백 4종
- 사진 방향 자동 대응 — 세로 사진은 필름이 세로로(퍼포레이션 좌우), 가로 사진 인스탁스는 우측 여백 카드(글씨 가로 스택/세로 회전 선택)
- 테두리 여백·하단 공간·글자 크기 배율, 글꼴 4종(시스템 폰트)

### 촬영 정보
- 카메라/렌즈/필름·기타/날짜/설정값(F·셔터·ISO·초점거리)/여러 줄 메모
- JPEG EXIF 장별 자동 입력 (자체 파서, 스캐너 기기명 가드, EXIF 배지로 원본 복원)
- 항목별 눈 아이콘 표시 토글, 장비 프리셋 팔레트
- 로고·서명 — 이미지 슬롯 + 텍스트 서명(필기체)

### 레이아웃
- 세로/가로 사진 별도 프로파일 (모든 배치 설정)
- 경계 표시 + 드래그 배치 — 수평/수직 스냅 가이드, 더블클릭 초기화

### 내보내기
- 인스타 비율 맞춤 (자동/4:5/1:1/1.91:1/9:16 스토리, 여백 색 선택)
- PNG/JPEG(품질), 긴 변 리사이즈, 폴더 지정 일괄 저장(Chromium)
- 파일 제목 순번 명명 — 제목(1), 제목(2)…

### 안전장치·기타
- EXIF 회전 자동 보정, 브라우저 캔버스 한계 자동 축소
- 모든 설정 localStorage 자동 저장 (사진은 어디에도 저장·전송되지 않음)
- 개인 설정 — 패널 폭·UI 배율
