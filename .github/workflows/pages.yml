name: Deploy to GitHub Pages

on:
  push:
    branches: [master, main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      # 빌드 의존성 없이 단일 HTML·외부 요청 금지·호환성 표식을 먼저 확인한다.
      - name: PhotoFrame 회귀 검사
        run: node tools/check.mjs
      # Pages가 꺼져 있으면 워크플로가 직접 활성화한다 (수동 설정 불필요)
      - uses: actions/configure-pages@v5
        with:
          enablement: true
      # 정적 파일 그대로 배포 — 빌드 단계 없음 (단일 HTML 원칙)
      - uses: actions/upload-pages-artifact@v3
        with:
          path: .
      - id: deployment
        uses: actions/deploy-pages@v4
